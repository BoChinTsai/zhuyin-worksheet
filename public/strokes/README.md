# Stroke data

The XML files in this directory are mirrored from the Ministry of Education stroke data exposed by `stroke.gh.miniasp.com/data/`.

The application uses these local files at runtime so the worksheet does not depend on a third-party CORS response.
